# 环境配置说明

本项目使用两套独立配置文件，**不用来回改同一个 .env**：

| 文件 | 用途 | 对应命令 |
|------|------|----------|
| `.env.local` | 本地 MySQL | `npm run start:dev` |
| `.env.production` | 阿里云 RDS | `npm run start` |

首次使用：

```bash
cp .env.local.example .env.local
cp .env.production.example .env.production
# 分别填入本地密码和阿里云 RDS 信息，填一次即可
```

本地 CORS 默认仅允许：`http://localhost:3000`
